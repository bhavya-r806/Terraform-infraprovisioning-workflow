name: Deploy Network

on:
  workflow_dispatch:
    inputs:
      cloud:
        type: choice
        options: [azure]
      environment:
        type: choice
        options: [prod]

jobs:
  deploy:
    uses: ./.github/workflows/terraform-reusable.yml
    with:
      cloud: ${{ inputs.cloud }}
      environment: ${{ inputs.environment }}
